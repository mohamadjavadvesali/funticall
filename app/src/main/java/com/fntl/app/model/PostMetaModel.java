package com.fntl.app.model;

import com.fntl.app.utils.MetaData;

public class PostMetaModel extends MetaData {


    public PostMetaModel(int pageIndex, int pageSize, int total) {
        super(pageIndex, pageSize, total);
    }
}
